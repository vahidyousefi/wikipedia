<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/open_link"
        android:icon="@drawable/ic_open_link"
        android:title="Wikis ..."
        app:showAsAction="never">

        <menu>

            <item
                android:id="@+id/wikiFunctions"
                android:icon="@drawable/ic_wikifunctions"
                android:title="WikiFunction"
                app:showAsAction="never" />

            <item
                android:id="@+id/wikiMeta"
                android:icon="@drawable/ic_wikimeta"
                android:title="WikiMeta"
                app:showAsAction="never" />

        </menu>

    </item>

    <item
        android:id="@+id/open_map"
        android:icon="@drawable/ic_places"
        android:title="Open map"
        app:showAsAction="never" />

    <item
        android:id="@+id/exit"
        android:icon="@drawable/ic_exit_poweroff"
        android:title="Exit"
        app:showAsAction="never" />

</menu>